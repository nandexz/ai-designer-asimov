- **Tailwind CSS** — utility-first CSS framework loaded via the Aura runtime resource bundle, used for all layout, spacing, color, and responsive styling
- **Google Fonts** — webfont delivery for the typography stack (Inter, Oswald, Playfair Display) preconnected and loaded via local CSS shim
- **Inter** — sans-serif font used as the default body typeface
- **Oswald** — condensed display font used for headings, eyebrows, and the AEX wordmark via the `.font-display` utility
- **Playfair Display** — italic serif font used for editorial accent quotes and the hero tagline via the `.font-accent` utility
- **Iconify** — icon web-component runtime (`<iconify-icon>`) used to render Solar icon set glyphs (search, heart, snowflake, scale, shield, radar, etc.) across the navigation, product cards, and footer
- **Solar Linear icon set** — icon pack consumed through Iconify for all interface icons
- **Google Analytics 4 (gtag.js)** — analytics tracker (property `G-2M6V79H761`) initialized via the bundled `dataLayer`/`gtag` snippet
- **Aura security firewall** — inline runtime guard that patches `localStorage`, `sessionStorage`, `document.cookie`, `fetch`, `XMLHttpRequest`, `sendBeacon`, and `WebSocket` to block Supabase auth-token leakage from the published preview
- **Supabase** — referenced as the storage origin for hero/preview imagery (`hoirqrkdgbmvpwutwuwj.supabase.co`); the firewall above protects against credential exposure to that host
- **IntersectionObserver API** — browser API driving the in-view scroll-reveal animation that toggles `.animate-on-scroll` elements
- **requestAnimationFrame** — browser API used to throttle the cinematic parallax updates on the hero blinds and immersive break background
- **CSS Keyframe Animations** — custom `animationIn` keyframe (translate + blur fade) that powers the hero entrance, doctrine reveal, sector cards, metrics, and inventory cards
- **Aura Build runtime** — the bundled `resource_3fa48481346f.es` module that ships Tailwind, image fallbacks, and other Aura platform behaviors
